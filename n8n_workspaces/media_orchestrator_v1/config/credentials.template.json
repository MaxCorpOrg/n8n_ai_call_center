{
  "credentials_needed": [
    {
      "name": "Telegram Bot",
      "type": "telegramApi",
      "required_for": [
        "MEDIA_AGENT_1 | Master Orchestrator TG (draft): Telegram Trigger",
        "MEDIA_AGENT_1 | Master Orchestrator TG (draft): Telegram Reply"
      ],
      "fields": [
        "accessToken"
      ]
    },
    {
      "name": "Mistral via OpenAI API",
      "type": "openAiApi",
      "required_for": [
        "MEDIA_AGENT_1 | Master Orchestrator TG (draft): Mistral (OpenAI Compat)",
        "MEDIA_AGENT_2 | Script Planner (draft): Mistral (OpenAI Compat)",
        "MEDIA_AGENT_4 | Kling Video (draft): Mistral (OpenAI Compat)",
        "MEDIA_AGENT_5 | Gemini Nano Banana Image (draft): Mistral (OpenAI Compat)"
      ],
      "fields": [
        "apiKey",
        "baseURL"
      ]
    }
  ],
  "notes": [
    "Agent 3 uses Pollinations URL fallback and does not require credentials.",
    "Replace REPLACE_KLING_API_KEY / REPLACE_TAVILY_API_KEY / REPLACE_GEMINI_API_KEY in workflow nodes after import.",
    "KB Sync Agent uses env vars KB_GITHUB_TOKEN (required) and N8N_PUBLIC_API_KEY (optional), not n8n Credentials.",
    "Memory Neuro Agent uses env vars MEMORY_GITHUB_TOKEN (required for github backend) and optional MEMORY_CONNECTOR_* URLs, not n8n Credentials."
  ]
}
