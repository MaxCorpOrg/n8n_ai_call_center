# 2026-06-12: контрольная точка по hard-stop правилу `абонент / абоненту / абонентам`

## Сделано

- Через `ElevenLabs Conversations API` перечитан разговор:
  - `conv_0401ktxtzpz2ftdv01cmb76c77ba`
- Подтверждено, что старого wording по `абонент` было недостаточно как priority-rule:
  - модель местами всё ещё могла уйти в трактовку `живой посредник`.
- Live `AI_CALL_AGENT_1` перепрошит точечным prompt-patch.
- В `Machine and screening rules` добавлен явный override:
  - любое сервисное употребление
    - `абонент`
    - `абоненту`
    - `абонентам`
  - немедленно перебивает ветку `useful intermediary`.
- Дополнительно закреплены примеры:
  - `сейчас же отправлю её абоненту`
  - `что передать абоненту?`
  - `есть что сообщить дополнительно?`
  - `нужно передать ещё что-то абоненту?`
  - `что бы вы хотели сказать абоненту?`
- После trigger-фразы агент должен:
  - не отвечать;
  - не продавать;
  - не предлагать SMS;
  - не оставлять номер менеджера;
  - делать `call_log(no_answer|busy)` и затем silent `end_call`.

## Текущая live-версия

- `agtvrsn_6701ktxx1b3efdca8r13zcj2sys2`

## Где лежит backup

- `backups/2026-06-12_abonent_hard_override_refresh/`

## На чем остановились

- Правило уже применено в live.
- Документация проекта и документация для агента обновлены.
- Нужна короткая практическая проверка на звонке, где линия говорит `абоненту` или похожую service-transfer фразу.

## Что делать дальше

1. Сделать один короткий тестовый звонок на screening / assistant / message-service кейс.
2. Проверить, что после первой trigger-фразы с `абоненту` агент больше не ведёт диалог.
3. Подтвердить по transcript:
   - `call_log(no_answer|busy)`
   - silent `end_call`
   - отсутствие spoken follow-up.

## Контекст для нового чата

- Рабочая ветка:
  - `codex/email-followup-agent-live`
- Главный свежий вывод:
  - слово `абонент` в сервисной реплике теперь считается не “подсказкой”, а жёсткой командой на завершение звонка.
