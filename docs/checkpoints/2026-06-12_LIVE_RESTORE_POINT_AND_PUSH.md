# Контрольная точка 2026-06-12: backup, commit, push

## Сделано

- Собрана локальная точка отката проекта и live-настроек в отдельную папку:
  - `/home/max/n8n_ai_call_center/backup/2026-06-12_live_agent_restore_point`
- В backup-пакет положены:
  - подтвержденный live JSON агента;
  - payload-ы последних patch-шагов;
  - снимок ключевой документации;
  - git branch/head/status/log;
  - patch несохраненных изменений;
  - состояние минимальных workflow в `n8n`.
- В документации зафиксировано, что рабочей live version на этой точке является:
  - `agtvrsn_0901ktxsrethemqr4prhkw701wr2`
- Убрано противоречие с промежуточной слишком жёсткой refusal-версией:
  - она рассматривается только как исторический эксперимент, а не как текущая цель.

## На чем остановились

- Локальная restore-point уже сохранена.
- Дальше можно безопасно продолжать одиночные тесты и prompt-правки, имея понятный rollback-набор.
- Минимальный outbound-контур остаётся на паузе до следующей команды.

## Что делать дальше

1. Делать следующий одиночный звонок только на текущей live version.
2. После нового live-теста сравнивать поведение уже с этой контрольной точкой.
3. Перед новым risky patch создавать соседний backup-каталог, а не перезаписывать этот.
