# 2026-06-13: смягчение агрессивного перехвата хода

## Что случилось

- На тестовом звонке:
  - `conv_3501kv0ev6y2fb3r7hmvyyp5cayw`
- агент перебивал собеседника и не давал закончить ответ:
  - после `М-м-м` сразу задавал новый вопрос;
  - на начале фразы `Ну, ва-` снова лез поверх реплики.

## Что подтвердил лог

- Live turn-настройки были слишком агрессивными:
  - `turn_timeout = 1.0`
  - `turn_eagerness = eager`
  - `speculative_turn = true`
  - `retranscribe_on_turn_timeout = false`
- Это делало агента слишком торопливым:
  - он перехватывал короткие паузы;
  - трактовал колебания как конец реплики;
  - продолжал продавать, не дождавшись человека.

## Что исправлено

- Новая live version:
  - `agtvrsn_1701kv0ezcqvejtaz1zpyjevmpqf`
- Turn-настройки смягчены:
  - `turn_timeout = 1.6`
  - `turn_eagerness = normal`
  - `speculative_turn = false`
  - `retranscribe_on_turn_timeout = true`
- Добавлены `interruption_ignore_terms`:
  - `м-м-м`
  - `угу`
  - `ага`
  - `ну`
  - `секунду`
  - `подождите`
  - `сейчас`
- В prompt добавлено прямое правило:
  - `м-м-м`, `ну`, `секунду`, `подождите`, незаконченные начала фраз вроде `ну, ва-`
    считаются признаком, что человек ещё держит слово;
  - на них нельзя забирать ход и задавать новый вопрос.

## Где лежит backup

- `backups/2026-06-13_interrupt_aggression_trim/`

## На чем остановились

- Агрессивный перехват хода в live уже ослаблен.
- Следующая проверка должна быть живым коротким звонком на тот же номер, чтобы увидеть:
  - перестал ли агент лезть поверх человека;
  - стал ли он дожидаться завершения колеблющейся реплики.

## Что делать дальше

1. Сделать ещё один короткий тестовый звонок.
2. Проверить:
   - даёт ли агент договорить;
   - не повторяет ли opener второй раз;
   - не лезет ли с новым вопросом на `м-м-м` и `ну...`.
3. После этого отдельно возвращаться к:
   - `call_log` bridge `404`
   - окончательной зачистке pre-opener rescue.
