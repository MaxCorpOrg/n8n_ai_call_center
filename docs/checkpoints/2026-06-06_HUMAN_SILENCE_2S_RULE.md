# 2026-06-06: правило `2s -> rescue -> 2s -> hangup`

## Сделано
- Логика тишины после живого ответа уточнена до финальной схемы:
  1. после opener и уже подтверждённого live-human ответа;
  2. если около `2` секунд нет осмысленного ответа;
  3. agent один раз говорит:
     - `Алло, меня слышно? Вы тут?`
  4. если после этого ещё около `2` секунд нет нормального ответа;
  5. agent сам:
     - `call_log(no_answer)`
     - silent `end_call`
- Повторять rescue-вопрос второй раз запрещено.
- Live-config:
  - `turn_timeout = 3.2`
  - `soft_timeout_config.timeout_seconds = 2.0`
  - `soft_timeout_config.message = "Алло, меня слышно? Вы тут?"`
  - `max_soft_timeouts_per_generation = 1`
- Новая live version:
  - `agtvrsn_9401kte963xcf2j87t1wervbdtv5`

## На чем остановились
- Новый звонок после этой правки ещё не запускался.
- Значит правило уже в live, но ещё не подтверждено свежим разговором.
- Контур по-прежнему стоит на паузе.

## Что делать дальше
1. Следующий одиночный speech-тест делать уже с этой версией.
2. Проверять:
   - rescue звучит через `~2` секунды;
   - не повторяется;
   - после новой тишины agent сам завершает звонок примерно ещё через `~2` секунды.
