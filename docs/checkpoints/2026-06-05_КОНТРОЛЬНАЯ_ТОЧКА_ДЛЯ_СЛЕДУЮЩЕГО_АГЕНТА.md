# 2026-06-05: контрольная точка для следующего агента

## Сделано
- Основной `n8n` уже работает на `Postgres`.
- Live колл-центр переведён в режим коротких одиночных тестов с обязательной паузой между ними.
- Точный двухфразный opener возвращён в live `Eleven Main`.
- Machine / voicemail path уже хорошо отрабатывает:
  - без sales-речи;
  - без spoken farewell;
  - с `call_log` и silent `end_call`.
- Traceability почти доведена:
  - `lead_id`
  - `source_record_key`
  - `phone_primary`
  - `eleven_conv_id`
- Последний найденный edge-case:
  - agent мог переиспользовать старый `conv_*` из prompt-примера;
  - буквальный пример удалён;
  - добавлен прямой запрет reuse `conv_*` из прошлого звонка/примера/tool-result.
- Текущая live version агента:
  - `agtvrsn_1001ktbys8ftfpys5gykctxrqka5`

## На чем остановились
- Минимальные workflow снова выключены:
  - `ELEVEN_OUTBOUND_CALL_BRIDGE (draft)` = `false`
  - `ELEVEN_TOOL_CALL_LOG_BRIDGE (draft)` = `false`
  - `ELEVEN_TOOL_CONTEXT_BRIDGE (draft)` = `false`
- `VOICE_INBOUND_AGENT (draft)` и dispatcher тоже не должны самопроизвольно стартовать в рамках этого цикла.
- `n8n-server-n8n-1` healthy.
- Открытый незакрытый вопрос сейчас только один:
  - проверить либо точный двухфразный opener на живом человеке,
  - либо подтвердить, что anti-reuse fix по `conv_*` действительно сработал на следующем звонке.

## Что делать дальше
1. Поднять только минимальные workflow:
   - `ELEVEN_OUTBOUND_CALL_BRIDGE (draft)`
   - `ELEVEN_TOOL_CALL_LOG_BRIDGE (draft)`
   - `ELEVEN_TOOL_CONTEXT_BRIDGE (draft)`
2. Сделать ровно один следующий звонок по `row_8`.
3. Снять transcript и `call_log`.
4. Если будет machine/voicemail:
   - проверить, что в `call_log` ушёл именно текущий `conv_*`, без reuse старого ID.
5. Если будет живой человек:
   - проверить, что первая spoken-реплика идёт ровно фиксированным двухфразным opener-блоком.
6. После звонка снова всё выключить и обновить:
   - `docs/knowledge_base/09_PROJECT_CHANGELOG_AND_STATE.md`
   - `документация_для_агента/02_ТЕКУЩЕЕ_LIVE_СОСТОЯНИЕ.md`

