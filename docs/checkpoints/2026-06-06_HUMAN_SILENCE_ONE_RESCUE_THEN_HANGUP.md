# 2026-06-06: один rescue-вопрос, потом быстрый сброс

## Сделано
- Логика human-silence после opener уточнена по пользовательскому правилу:
  - при тишине около `3` секунд после opener и уже подтверждённого live-human ответа;
  - agent задаёт только один rescue-вопрос:
    - `Алло, меня слышно? Вы тут?`
  - если после этого вопроса ещё около `2` секунд нет осмысленного ответа;
  - agent должен:
    - `call_log(no_answer)`
    - `silent end_call`
- Повторять rescue-вопрос второй раз запрещено.
- Это закреплено в live `Main`:
  - в prompt;
  - и через `soft_timeout_config`
- Текущая live version:
  - `agtvrsn_3301kte2vf9se8psr98j94hk24z6`

## На чем остановились
- Новый звонок после этой уточняющей правки ещё не запускался.
- Значит правило уже в live, но ещё не подтверждено свежим speech-тестом.
- Контур всё ещё стоит на паузе.

## Что делать дальше
1. Следующий одиночный speech-тест запускать уже с этой версией.
2. Проверять:
   - rescue звучит один раз;
   - повторов нет;
   - после новой тишины agent сам завершает звонок без второй попытки.
