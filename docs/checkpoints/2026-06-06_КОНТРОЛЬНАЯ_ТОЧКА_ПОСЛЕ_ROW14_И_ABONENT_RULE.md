# 2026-06-06: контрольная точка после `row_14` и жёсткого правила по `абонент`

## Сделано
- Зафиксирован жёсткий live-rule:
  - любые сервисные фразы со словами `абонент`, `абоненту`, `абонентам`
  - особенно `что передать абоненту?`
  теперь сразу считаются автоответчиком / message-service
- Для этого обновлён live `Main` в ElevenLabs:
  - `version_id = agtvrsn_4301ktee0x3kf8es9y3f950rjzr8`
- Ранее отдельно уже были применены fix после `row_11`:
  - убран конфликт global `soft_timeout` с `human-answer gate`
  - live `ELEVEN_TOOL_CALL_LOG_BRIDGE` получил нормализацию malformed `conv_*`
- Выполнены два следующих одиночных цикла:
  - `row_13`
  - `row_14`
- Оба цикла закончились одинаково:
  - внешний webhook отвечал `HTTP 200`
  - body был пустой
  - relay-host `151.241.228.232` отдавал `502`
  - разговор в Eleven не создавался
- После каждого цикла минимальные workflow возвращались в паузу.

## На чем остановились
- Речевое поведение агента после последних правок пока не проверено новыми разговорами, потому что два цикла подряд не дошли до stage создания conversation.
- Значит текущий blocker сейчас уже не в prompt и не в `call_log`, а в outbound relay / upstream path.

## Что делать дальше
1. Не делать новый prompt-patch сразу.
2. Сначала разобрать relay `502`:
   - посмотреть timeout-path;
   - понять, меняется ли upstream response;
   - проверить, не нужно ли чуть поднять timeout или поправить relay logging/handling.
3. Только после стабилизации outbound path делать следующий одиночный звонок по очереди.
4. На следующем успешном разговоре проверить:
   - не выстреливает ли rescue слишком рано;
   - доезжает ли нормальный текущий `eleven_conv_id`;
   - если слышим `что передать абоненту?` или любую похожую фразу с `абонент/абоненту/абонентам`, agent должен завершать сразу без продажного текста.
