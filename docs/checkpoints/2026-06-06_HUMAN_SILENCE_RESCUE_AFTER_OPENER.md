# 2026-06-06: добавлен rescue-вопрос после тишины у живого человека

## Сделано
- По обратной связи пользователя изменена логика паузы после opener.
- Раньше после `human answer -> opener -> silence/...` agent должен был быстро завершать звонок.
- Теперь для этой ветки включено новое поведение:
  - если уже был подтверждён живой человек;
  - и после opener около `3` секунд нет осмысленного ответа;
  - agent один раз спрашивает:
    - `Алло, меня слышно? Вы тут?`
- Это закреплено в двух местах:
  1. в prompt;
  2. в `turn.soft_timeout_config`
- Текущая live конфигурация:
  - `turn_timeout = 3.2`
  - `soft_timeout_config.timeout_seconds = 3.0`
  - `soft_timeout_config.message = "Алло, меня слышно? Вы тут?"`
  - `max_soft_timeouts_per_generation = 1`
- Исключение действует только на human-ветке после opener.
- На machine/IVR/voicemail/message-service/screening/music/ringback этот rescue запрещён.
- Новая live version:
  - `agtvrsn_0401kte1n4fhek8snba466ra39t0`

## На чем остановились
- Новый звонок после этой правки ещё не запускался.
- Значит правило уже в live, но пока не подтверждено новым одиночным тестом.
- Весь контур по-прежнему стоит на паузе.

## Что делать дальше
1. Следующий одиночный тест делать уже с этой версией.
2. Слушать три вещи:
   - rescue-вопрос звучит вовремя и коротко;
   - не срабатывает на machine-path;
   - после rescue не ломается `call_log`.
