# Live-настройка ElevenLabs агента (RU)

## Назначение

Этот документ фиксирует текущее рабочее состояние live-агента ElevenLabs для телефонии LipoLong.

Актуальность снимка: `2026-03-20`.

## Идентификаторы

- Agent name: `AI_CALL_AGENT_1`
- Agent ID: `agent_8801kgybyekned2a8yae6rp8hk3q`
- Branch ID: `agtbrch_7801kgybyg9nesrbv64y078pazq0`

## Что подключено

- Телефонный номер: `+79923298897`
- Provider: `sip_trunk`
- Inbound: `true`
- Outbound: `true`
- Tools:
  - `context_fetch`
  - `call_log`

## Стартовая фраза

`first_message` в live сохранена как обязательная бизнес-формулировка и не должна меняться без отдельного решения:

`Здравствуйте, наша компания является официальным представителем липолитика премиум класса ЛипоЛонг, предлагаем вам сотрудничество с нашей компанией на выгодных условиях. А еще, сотрудничая с нами, вы можете быть уверены на 100%, что получаете оригинальную продукцию и не рискуете попасть на подделку`

## Текущая voice/LLM-конфигурация

- LLM: `gemini-2.5-flash`
- TTS model: `eleven_flash_v2_5`
- Voice ID: `FZGeNF7bE3syeQOynDKC`
- Speed: `1.2`
- Turn eagerness: `eager`
- Turn timeout: `4.0`

## Настройки перебивания

- `disable_first_message_interruptions = true`
- Из `conversation.client_events` удалено событие `interruption`

Практический эффект:
- стартовая фраза должна договариваться до конца;
- обычные реплики агента тоже не должны прерываться пользовательским `алло` или ранней репликой.

## Почему конфигурация была изменена

В live-тестах были выявлены две основные проблемы:
- стартовая фраза прерывалась клиентом на первом же `алло`;
- агент местами отвечал с лишней паузой и повторял один и тот же вопрос вместо короткого прояснения.

Для исправления без изменения бизнес-формулировки `first_message` были приняты следующие меры:
- ускорен voice-движок;
- LLM переведен на более быструю и актуальную Flash-модель;
- уменьшен `turn_timeout`;
- в `system prompt` добавлены правила анти-повтора и короткой реакции на `алло / не слышу / о чем звонок / кто вы / где вы`.

## Наблюдения по последним тестам

Типовые результаты последних звонков:
- агент корректно завершает диалог как `not_target`, если клиент не работает с коррекцией фигуры и инъекционными методиками;
- часть ранних тестов показала, что длинная стартовая фраза сама по себе создает напряжение в первые секунды разговора;
- основная остаточная задержка обычно связана не с TTS, а с turn-taking и LLM-решением на нечетких репликах клиента.

## Что не менять без отдельного теста

- `first_message`
- SIP trunk / номер / inbound-outbound настройки
- webhook URL tools
- `voice_id`

## Что можно тестировать дальше

- дополнительные правила в `system prompt` для обработки неразборчивой связи;
- корректировку логики follow-up после отказа;
- запасной LLM provider в Backup LLM configuration;
- более короткие уточняющие ответы на первые 2-3 реплики клиента.
